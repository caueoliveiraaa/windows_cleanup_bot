# Write a program that takes a string and prints the first and
# last characters.

var = input('Type in string: ')
print(f'First character of {var}:', var[0])
print(f'Last character of {var}:', var[-1])